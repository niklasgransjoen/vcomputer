using System;
using VComputer.Components;

namespace VComputer.Interface.Components
{
    public sealed class RAMInitializer : IRAMInitializer
    {
        public RAMInitializer()
        {
        }

        public void InitializeRAM(ReadOnlySpan<bool[]> memory)
        {
        }
    }
}